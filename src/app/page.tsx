"use client";

import { AppContainer } from "@/app/styles";
import Chats from "@/components/chats";

const Home = () => {
    return (
        <AppContainer>
            <Chats />
        </AppContainer>
    );
};

export default Home;
